import { CapacitorConfig } from '@capacitor/core';

const config: CapacitorConfig = {
  appId: 'com.ansh.ankhgenvault',  // Changed to valid format
  appName: 'ankh-gen-vault',
  webDir: 'dist'
};

export default config;
